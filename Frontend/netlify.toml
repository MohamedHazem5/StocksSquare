[build]
  command = "npm run build"
  publish = "dist"

# Proxy API through Netlify (avoids browser CORS to Somee)
[[redirects]]
  from = "/api/*"
  to = "https://www.StockSquares.somee.com/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
